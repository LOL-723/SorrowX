# 更新说明

本文件记录对使用者、维护者或架构有影响的更新。每个发布版本应在合并对应提交时新增一个条目；未发布改动先写入 `Unreleased`。

## Unreleased

### Added

- 新增 session 级记忆状态文件，记录原始记录序号、摘要覆盖范围、刷新状态和失败抑制版本。
- 新增后台滚动摘要：以已有摘要与新增对话为输入，每批最多合并 10 条记录，摘要上限保持 300 字。
- 新增记忆可靠性测试，覆盖连续对话回退、摘要失败抑制、滚动合并、并发写入和旧记录兼容。

### Changed

- 长期记忆提交由 `AgentLoopEngine` 移至 `AgentRuntime`；成功记录现包含 schema 版本、session 内序号、运行 ID 和创建时间。
- 下一轮 Agent 运行改为加载已解析上下文：摘要有效时读取摘要；摘要未覆盖最新记录时追加有限原始记录回退。
- `CheckMemory` 现显示摘要覆盖范围、刷新状态及是否启用回退；`UpdateMemory` 使用滚动摘要并显示最终覆盖序号。

### Fixed

- 修复同一 Daemon 内摘要未即时刷新导致连续对话无法稳定读取上一轮结果的问题。
- 修复摘要生成失败可能被重复触发的风险：无新成功记录时不会自动重试。
- 移除 Daemon 启动时的隐式摘要刷新，避免启动成为重试入口。

### Architecture

- 当前记忆链路图见 [architecture/memory-flow.md](architecture/memory-flow.md)。

