# 架构文档

此目录存放当前系统链路与关键约束。图使用 Mermaid 编写，可由 GitHub 直接渲染。

维护规则：

- 影响数据流、运行边界、失败策略或并发策略的提交，必须同步更新对应链路图。
- 新模块使用独立的 `<module>-flow.md`；若某图被重大重构替代，在原文件顶部标记替代关系，而不是覆盖历史结论。
- 每次架构图变更都在根目录 [CHANGELOG.md](../CHANGELOG.md) 的 `Architecture` 小节链接说明。

当前文档：

- [记忆链路](memory-flow.md)

